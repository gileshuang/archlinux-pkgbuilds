diff -uNr a/src/zx_fbdev.c b/src/zx_fbdev.c
--- a/src/zx_fbdev.c	2026-03-24 09:59:16.000000000 +0800
+++ b/src/zx_fbdev.c	2026-03-24 10:45:49.149150928 +0800
@@ -205,6 +205,10 @@
 #else
 #if DRM_VERSION_CODE < KERNEL_VERSION(6, 2, 0)
     info = drm_fb_helper_alloc_fbi(helper);
+#elif DRM_VERSION_CODE >= KERNEL_VERSION(6, 19, 0)
+    /* Linux 6.1+ : fb_info 内嵌在 helper 中，使用 fill_info 填充 */
+    info = helper->info;
+    drm_fb_helper_fill_info(info, helper, sizes);
 #else
     info = drm_fb_helper_alloc_info(helper);
 #endif
diff -uNr a/src/zx_pcie.c b/src/zx_pcie.c
--- a/src/zx_pcie.c	2026-03-24 09:59:16.000000000 +0800
+++ b/src/zx_pcie.c	2026-03-24 10:14:18.293863383 +0800
@@ -418,7 +418,15 @@
         return;
     }
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 19, 0)
+    struct drm_atomic_state *state = drm_atomic_state_alloc(fbdev->helper.dev);
+    if (state) {
+        drm_fb_helper_restore_fbdev_mode_unlocked(&fbdev->helper, state);
+        drm_atomic_state_put(state);
+    }
+#else
     drm_fb_helper_restore_fbdev_mode_unlocked(&fbdev->helper);
+#endif
 }
 
 #if DRM_VERSION_CODE < KERNEL_VERSION(4,11,0)
